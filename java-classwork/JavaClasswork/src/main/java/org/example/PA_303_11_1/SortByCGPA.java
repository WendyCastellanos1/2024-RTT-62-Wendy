package org.example.PA_303_11_1;

// Helper class implementing Comparator interface

//public class SortByCGPA implements java.util.Comparator<Student> {
//
//        // method sorts in descending order by CGPA
////        public int compare(Student a, Student b)
////        {
//////            if (Double.compare(a.getCGPA(), b.getCGPA()) == 0) {
//////                //Gpas are the same, so these two names alphabetically, ascending
//////                if (new SortByName() == 0) {
//////                    //Names are the same, so sort by ID ascending
//////                    return new SortById(a, b);
//////                }else {
//////                    return (new SortByName(a, b));
//////                }
//////            }else{
//////                return Double.compare(b.getCGPA(), a.getCGPA());
//////            }
//       }
////}
